import { storiesOf } from '@storybook/react-native';
import React from 'react';

import { BookcoverPlaceholder } from './BookcoverPlaceholder';

storiesOf('BookcoverPlaceholder', module).add('Default', () => (
  <BookcoverPlaceholder />
));
